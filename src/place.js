if (entities) {
    twitterUserName ? setTwitterURL(twitterUserName) : console.log('No Twitter');
  youtubeChannelID ? setYouTubeURL(youtubeChannelID): console.log('No YouTube');
  InstagramUsername ? setinstagramURL(InstagramUsername) : console.log('No Instagram');
  soundCloudID ? setsoundCloudURL(soundCloudID) : console.log('No Soundcloud');
  }